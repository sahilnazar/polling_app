# Use a direct PostgreSQL URL (postgresql://), not prisma+postgres
# For local dev: start Postgres (e.g. Docker) and set:
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/polling_app?schema=public"

# For docker-compose, the backend service will use:
# DATABASE_URL="postgresql://postgres:postgres@db:5432/polling_app?schema=public"
